{
  "scripts": {
    "format": "prettier --write --ignore-unknown .",
    "build": "sass --no-source-map src/scss/style.scss src/css/style.css && shx rm -rf dist && shx mkdir -p dist && cd src && bestzip ../dist/safex.zip css/style.css"
  },
  "devDependencies": {
    "bestzip": "^2.2.1",
    "prettier": "^3.6.2",
    "prettier-plugin-css-order": "^2.1.2",
    "sass": "^1.92.1",
    "shx": "^0.4.0"
  }
}
